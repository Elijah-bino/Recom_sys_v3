// After you deploy the API on Render, paste its HTTPS URL here (no trailing slash).
// Leave empty for local development (UI uses http://127.0.0.1:8010 or saved value).
window.__RECOMSYS_API_BASE__ = "";
